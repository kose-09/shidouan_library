<div class="container-fluid">
  <div class="row m-2">
    <div class="col-2 border p-3">
      <%= render 'search_form' %> 
    </div>
    <div class="col-9 offset-1">
      <h5>投稿一覧</h5>
      <%= render 'list', posts: @posts %>
      <%= paginate @posts %>
    </div>
  </div>
</div>



